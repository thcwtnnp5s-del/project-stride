// StrideApp.swift
// Project Stride — app target
//
// F-01 scope: the shell only. No gameplay, no HealthKit, no production UI.

import SwiftUI

@main
struct StrideApp: App {
    var body: some Scene {
        WindowGroup {
            RootPlaceholderView()
        }
    }
}
