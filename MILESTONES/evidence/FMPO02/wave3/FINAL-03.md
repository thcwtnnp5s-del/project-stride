# FINAL-03 — visible equipment: universal and honest?

The seam is real and the tables are genuinely two-axis. It is not universal, and in three places it is not honest.

1. **BLOCKER — Craft still draws the base body.** `craft_screen.dart`:1731–1750 builds its `AmbientStage` from `AmbientAssets.scenes` / `restFrame` / `activityLoopFor`; `TravelerArt` appears nowhere in the file. Wear the Bronze Chestplate, open a smithing queue, and the man at the forge is back in the white shirt and green vest — the owner's failure 4, unfixed, on the screen the player watches longest.
   *Fix:* route it through `idleScenesFor` / `restFrameFor` / `restFootprintFor` exactly as `location_stage.dart`:174–216 does.

2. **BLOCKER — no device evidence for any armoured loadout.** `screen_evidence_test.dart` equips only training sword/tunic/axe/pickaxe (:207, :945, :961, :993). All 41 `review/device/` screens are the starting loadout; `combat/gear_*.png` are VAWO01's three **base-body** weapon states; `gfcp_gather_plate.png` is the Gather commit *plate*, not armour. The round's central claim rests on a unit test over asset path strings, and A-3 gives the verdict to the device.
   *Fix:* capture adventure / gather / combat / craft at 393×852 in each of plate, jerkin and coat before this reaches the phone.

3. **BLOCKER — the Traveler changes hair colour with his gear.** Every `armor.jerkin` strip is a **ginger** man; base, plate and coat are brown. The re-dressed mining strips then carried that ginger head onto brown bodies: `traveler_coat_bronzepick_mine_*` is ginger while `traveler_coat_forage`, `_idle_breathe` and every coat combat track are brown; `traveler_base_bronzepick_mine_*` is ginger while `traveler_base_bronzeaxe_woodcut_*` — same body, one node away — is brown. Equip a Bronze Pickaxe and the starting character becomes a different man; equip a Bronze Axe and he changes back.
   *Fix:* remap the hair cluster to the canonical brown ramp (deterministic, A-2-legal), or reroll the jerkin state from the canonical figure.

4. **BLOCKER — the plate work loops face away from the resource, and strike on the carry pose.** `AmbientAssets.worksEast` is `false` for every skill, so the prop stands west (`stage/mine_copper_f3.png`: boulder left of the figure). `traveler_plate_bronzepick_mine` and `traveler_plate_bronzeaxe_woodcut` are the only work loops authored **east-facing** — his back is to the ore — and four of their eight frames repeat one pose. `strikeFrame: 0` (traveler_art.dart:246, :261) then fires the gather beat and the audio cue on the frame where the tool is shouldered.
   *Fix:* re-author both west-facing from the jerkin source and re-measure. `measure-reach.js` reports the *rightmost* opaque column — the wrong end for a west-working loop.

5. **BLOCKER — one mining frame is a solid white blob.** `traveler_plate_bronzepick_mine_f4.png` carries an opaque pure-white wedge standing on the ground beside the pick — not the base woodcut loop's clean crescent, an amorphous shape. One frame in eight at 6 fps against a dark mine wall: it strobes. *Fix:* key it out, or drop f4 and run the loop at seven frames.

6. **SHOULD-FIX — an armoured body on *training* tools is drawn holding bronze.** No `tool.*.steel` row exists for any armoured body, so `gatherStripFor` (:420–427) falls through to the bronze strip for every armoured player on starter tools — reachable the moment a Wolfhide Jerkin is crafted. The banned lie was reverting to the shirt; nobody authorised inflating to gear the player does not own. *Fix:* prefer the base-body steel loop over the same-body bronze one, or author the six armoured steel-tool strips.

7. **SHOULD-FIX — one Bronze Sword, three different weapons.** Sampled from the packaged idles: base `#ae4f22`/`#eaaf6e` (muted copper), plate `#f69027` (saturated flame — it reads as a burning blade and no crossguard survives), coat a flat tapered orange stick that reads as a wooden stave. `bronze_sword.png` — a gold-bronze blade with crossguard and pommel — matches none of them. *Fix:* remap all three blade clusters to the base set's copper ramp.

8. **SHOULD-FIX — the armour figures contradict their own item icons.** `bronze_chestplate.png` is a polished bronze cuirass; the whole plate family is **brown leather** with soft round pauldrons and no metal. `bearhide_coat.png` is a brown fur coat; the `armor.coat` figure is a grey wool greatcoat. *Fix:* recolour the two bodies to their icons, or re-author the two icons.

9. **SHOULD-FIX — the starting loadout alone has no Brace stance.** FMPO02 authored base-unarmed, base-bronze and all nine armoured braces; `CombatAssets.traveler` (tunic + Training Sword) has none, so `combat_choreography.dart`:249 holds the planted idle. The one player who has never seen Brace is the one it is teaching. *Fix:* author `traveler_base_steel_brace_f0..5`.

10. **SHOULD-FIX — the Inventory figure never holds the weapon or the tool.** `figureFor` keys on armour alone, so `inventory.png` shows an empty-handed man 12 dp from a slot reading "WEAPON / Training Sword": in the case built to answer "what am I wearing", two of three slots are invisible on the body. *Fix:* key the standing figure on body × weapon class, reusing the combat idle f0 that already exists for all twelve pairs.

11. **NOTES.** Ghost gear in two staggers — `plate_steel_stagger` f2–f4 and `coat_bronze_stagger` f6–f8 leave the blade floating clear of the hand with no arm reaching it. Crafting your first armour costs thirteen ambient studies: `idleScenesFor` swaps the base set's fifteen Traveler scenes for two. `combat_stage.dart`:321–323 rebinds art on `enemyId` but not on `widget.equipment` — latent, add the check. **Clean negatives:** I hashed every `traveler_*`/`activity_*` frame in `ambient/`, `combat/` and `sprite/` against its own horizontal flip and found no match — no mirrored strips, no flipped light; and the training sword on an armoured body is honest (`weaponSteel` → a plain steel blade). `PixelIcons.travelerSouth` is now dead and should go, so no future surface bypasses the seam.

**Verdict — REJECT.** Eight of ten contexts project, but Craft still shows the shirt, the Traveler changes hair colour with his pickaxe, the plate work loops swing away from the rock around a white strobing frame, and not one pixel of armour has been rendered at device size.
